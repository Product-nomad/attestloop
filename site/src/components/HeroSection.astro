---
---

<section
  class="min-h-[100svh] flex items-center justify-center px-6 sm:px-8 py-18 sm:py-24"
>
  <div class="max-w-[42rem] mx-auto text-center fade-in">
    <p
      class="text-xs sm:text-sm font-mono uppercase tracking-[0.18em] text-accent mb-6"
    >
      An agentic regulatory attestation pipeline
    </p>

    <h1
      class="font-serif font-normal text-text-primary text-[2.25rem] sm:text-[3.5rem] leading-[1.1] tracking-tight mb-6 sm:mb-8"
    >
      Four questions every compliance team gets asked.
    </h1>

    <p
      class="text-base sm:text-lg text-text-secondary leading-relaxed mb-10 sm:mb-12"
    >
      Attestloop is a multi-agent regulatory attestation pipeline. v2.0.0
      ships five LLM-driven agents — Classifier, Extractor, Mapper, Critic,
      Clarifier — connected by a typed state machine with conditional
      routing and parallel execution. Currently a research artefact
      demonstrating the approach against the EU AI Act and NIST AI RMF.
      Total cost per attestation: $2.09. Total wall-clock: 13 minutes.
      Open source under Apache 2.0.
    </p>

    <div class="flex flex-col sm:flex-row gap-3 sm:gap-4 justify-center">
      <a
        href="#how-it-works"
        class="inline-flex items-center justify-center px-6 py-3 bg-bg-elevated border border-border-default text-text-primary text-sm hover:bg-bg-overlay hover:border-accent transition-colors duration-150"
      >
        See how it works
      </a>
      <a
        href="/writeup"
        class="inline-flex items-center justify-center px-6 py-3 border border-border-subtle text-text-secondary text-sm hover:text-text-primary hover:border-border-default transition-colors duration-150"
      >
        Read the writeup
      </a>
    </div>

    <p class="mt-10 text-xs text-text-muted">
      Currently a research artefact built by Simon Newton. v2.0.0 — see
      GitHub for source.
    </p>
  </div>
</section>
