Canonical v6 run. Orchestration via LangGraph state machine, with the
Critic agent for second-pass review of low-confidence mappings, the
Clarifier agent for ambiguous Classifier decisions (not triggered on
this URL — Classifier returned high-confidence in_scope), and 8-way
concurrent Mapper execution. This is the artefact the writeup should
reference as "v6."

Pair with [`v6_v5_equivalent_clean/`](../v6_v5_equivalent_clean/) for
the v5↔v6 like-for-like comparison: both runs were generated by the
v6 codebase with the same Commission Guidelines URL and only the
`PipelineConfig` differs, so the deltas isolate the orchestration
impact.

Reproduce with:

```bash
uv run python -m attestloop \
    --url https://ec.europa.eu/newsroom/dae/redirection/document/112367 \
    --regulation eu_ai_act \
    --framework nist_ai_rmf \
    --config v6
```
